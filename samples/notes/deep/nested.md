# Deeply nested

You've found the deepest file in the samples directory.

The sidebar should show this at `samples/notes/deep/nested.md`, three dirs
deep. Click [back to the kitchen sink](../random-thoughts.md) to return, or
jump straight to the [README](../../README.md).

## Why this exists

- To make the file tree actually look like a tree.
- To let you verify the filter input cuts through nested directories.
- To prove you can navigate back up via `..`-style relative links.

## Relative asset across parents

Same `logo.svg`, resolved from three directories deep:

![logo](../../images/logo.svg)

## And a math tidbit

Euler's identity is still one of the prettier equations:

$$
e^{i\pi} + 1 = 0
$$
