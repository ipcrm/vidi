# Screenshots

These are referenced from `../index.html`. Capture from a running `pnpm tauri:dev`
session on macOS at the default window size:

- `reader.png` — main reader view with a real README rendered (e.g. this repo's)
- `sidebar-toc.png` — sidebar showing both Files and Contents sections populated
- `tabs.png` — three or four tabs open, including one with the overflow picker visible
- `search.png` — Search panel (⌘⇧F) with query and a few hits

Recommended size: 1600×1000 @ 2x (so the file is 3200×2000). PNG, no alpha.
